{
  "name": "shaaban-educational-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Mr. Shaaban Educational Platform - single-teacher LMS (React + Express + MongoDB)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "node server/src/utils/seed.js",
    "build": "npm run build --prefix client",
    "seed:demo": "node server/src/utils/seed.js --demo --reset",
    "reset": "node server/src/utils/seed.js --reset"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
